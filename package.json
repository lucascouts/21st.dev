{
  "name": "@lucascouts/21st.dev",
  "version": "1.1.0",
  "type": "module",
  "description": "Magic MCP UI builder - Secure MCP server for 21st.dev component generation",
  "homepage": "https://21st.dev/magic",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lucascouts/21st.dev.git"
  },
  "bin": {
    "magic": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "mcp",
    "model-context-protocol",
    "ai",
    "UI",
    "frontend",
    "21st.dev",
    "magic",
    "kiro"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js --format=esm --packages=external",
    "build:bun": "bun build src/index.ts --outdir dist --target node",
    "build:node": "tsc && shx chmod +x dist/*.js",
    "build:prod": "bun run build:bun && bun test",
    "debug": "bun run build:bun && npx @modelcontextprotocol/inspector bun dist/index.js DEBUG=true",
    "start": "node dist/index.js",
    "start:bun": "bun dist/index.js",
    "prepare": "npm run build",
    "dev": "bun --watch src/index.ts",
    "test": "bun test"
  },
  "author": "@inclulucascouts",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "open": "^11.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "fast-check": "^4.6.0",
    "shx": "^0.4.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.1.0"
  }
}
